package kr.arcadia.arcRootFinding.core

import java.nio.file.Path

class QueryEngine(
    wnmOrCchPath: Path
) {
    fun route(sx: Int, sy: Int, sz: Int, tx: Int, ty: Int, tz: Int): List<Int> { /* CCH Up-Down 탐색 + 언패킹 */ TODO() }
}